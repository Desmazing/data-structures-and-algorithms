# in math module, mat.degrees(angle) conv angle to degrees
# math.radians(angle) conv angle to radians

import math

rad = int(input('Enter angle in radians: '))
deg = math.degrees(rad)
print(deg)


deg = int(input('Enter angle in degrees: '))
rad = math.radians(deg)
print(rad)
